!function ($) {

    $(function () {
        // datetime-picker formatting
        jQuery('.datetime-picker').datetimepicker({
            format: 'm/d/Y h:i:s A'
        });

    });
}(window.jQuery);